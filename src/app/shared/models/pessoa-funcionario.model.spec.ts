import { PessoaFuncionario } from './pessoa-funcionario.model';

describe('PessoaFuncionario', () => {
  it('should create an instance', () => {
    expect(new PessoaFuncionario()).toBeTruthy();
  });
});

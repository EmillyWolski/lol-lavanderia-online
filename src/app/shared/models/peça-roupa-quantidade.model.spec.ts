import { PeçaRoupaQuantidade } from './peça-roupa-quantidade.model';

describe('PeçaRoupaQuantidade', () => {
  it('should create an instance', () => {
    expect(new PeçaRoupaQuantidade()).toBeTruthy();
  });
});
